Region Raster Masks should always be handled as .png files.
Never convert raster masks to svg
Never attempt to render raster masks as a <path> elements.
In the DM session viewer, the region/room raster masks should always be imported as a .png and should never be converted to/rendered as a .svg or <path> element.
Use the component names defined in COMPONENTS.md when discussing or modifying the UI. Refer to that glossary before introducing new labels for DM Session Viewer sub-panels, landing page sections, or map creation tooling.
